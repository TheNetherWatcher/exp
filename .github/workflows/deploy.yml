name: Trigger Webhook

on:
  push:
    branches:
      - main

jobs:
  trigger-webhook:
    runs-on: ubuntu-latest
    steps:
      - name: Send Webhook Trigger
        run: |
          curl -X POST https://8d12-37-19-205-148.ngrok-free.app/webhook \
          -H "Content-Type: application/json" \
          -d '{}'
